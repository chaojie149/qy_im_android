<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <ImageView
            android:id="@+id/ivAdd"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:paddingStart="@dimen/margin_screen_old"
            android:paddingTop="12dp"
            android:paddingEnd="8dp"
            android:paddingBottom="12dp"
            android:src="@mipmap/icon_input_phone" />

        <EditText
            android:id="@+id/etInputTelephone"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:layout_weight="1"
            android:background="@null"
            android:hint="@string/tip_telephone_add"
            android:inputType="phone"
            android:paddingTop="12dp"
            android:paddingRight="@dimen/margin_screen_old"
            android:paddingBottom="12dp"
            android:textColor="@color/text_black"
            android:textSize="15sp" />

    </LinearLayout>

    <View
        android:id="@+id/vLine"
        style="@style/View_Line"
        android:layout_marginStart="@dimen/margin_screen_old"
        android:layout_marginEnd="@dimen/dp15" />

</LinearLayout>